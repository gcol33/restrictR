% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{is_valid}
\alias{is_valid}
\title{Test Whether a Value Satisfies a Validator}
\usage{
is_valid(validator, value, ...)
}
\arguments{
\item{validator}{a \code{restriction} object created by \code{\link[=restrict]{restrict()}}.}

\item{value}{the value to validate.}

\item{...}{context arguments passed to the validator (e.g. \code{newdata = df}),
the same as a direct validator call.}
}
\value{
A length-1 logical.
}
\description{
Non-throwing predicate: returns \code{TRUE} when \code{value} passes every step of
\code{validator}, \code{FALSE} otherwise. Use it to branch on validity instead of
wrapping a validator call in \code{\link[=tryCatch]{tryCatch()}}.
}
\details{
Like \code{\link[=validation_errors]{validation_errors()}}, only validation failures are caught;
a missing context dependency still raises so a calling mistake is not
mistaken for invalid data.
}
\examples{
v <- restrict("x") |> require_numeric(no_na = TRUE)
is_valid(v, 1:5)        # TRUE
is_valid(v, c(1, NA))   # FALSE

}
\seealso{
\code{\link[=validation_errors]{validation_errors()}} for the failure messages.

Other core:
\code{\link[=as_contract_block]{as_contract_block()}},
\code{\link[=as_contract_text]{as_contract_text()}},
\code{\link[=fail]{fail()}},
\code{\link[=require_custom]{require_custom()}},
\code{\link[=restrict]{restrict()}},
\code{\link[=validation_errors]{validation_errors()}}
}
\concept{core}
