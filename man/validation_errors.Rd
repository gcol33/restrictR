% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{validation_errors}
\alias{validation_errors}
\title{Collect Validation Errors Without Throwing}
\usage{
validation_errors(validator, value, ...)
}
\arguments{
\item{validator}{a \code{restriction} object created by \code{\link[=restrict]{restrict()}}.}

\item{value}{the value to validate.}

\item{...}{context arguments passed to the validator (e.g. \code{newdata = df}),
the same as a direct validator call.}
}
\value{
A character vector of failure messages, or \code{character(0)} if the
value satisfies every step. Each element is the full path-aware message
for one failing step.
}
\description{
Runs a validator against a value and returns the validation messages
instead of raising an error. Every step is checked (as with
\code{.on_fail = "all"}), so all violations are reported in one pass.
}
\details{
Only validation failures are caught. A usage error, such as a
missing context dependency declared by a formula step, still propagates
so the calling mistake is not silently reported as invalid data.
}
\examples{
v <- restrict("x") |> require_numeric() |> require_positive()
validation_errors(v, 5)       # character(0)
validation_errors(v, c(-1))   # one message

}
\seealso{
\code{\link[=is_valid]{is_valid()}} for a logical predicate.

Other core:
\code{\link[=as_contract_block]{as_contract_block()}},
\code{\link[=as_contract_text]{as_contract_text()}},
\code{\link[=fail]{fail()}},
\code{\link[=is_valid]{is_valid()}},
\code{\link[=require_custom]{require_custom()}},
\code{\link[=restrict]{restrict()}}
}
\concept{core}
