% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restrict.R
\name{restrict}
\alias{restrict}
\title{Create a Composable Validator}
\usage{
restrict(name)
}
\arguments{
\item{name}{character(1) name used in error messages (e.g. \code{"newdata"}).}
}
\value{
A \code{restriction} object (callable function) with no validation steps.
}
\description{
Creates a callable validation object that accumulates checks via the base
pipe operator \verb{|>}. The resulting object behaves like a function: call it
with a value to validate.
}
\section{Calling convention}{


Validators accept \code{value} as the first argument, plus context via named
arguments in \code{...} or as a named list in \code{.ctx}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{require_pred(out, newdata = df)
require_pred(out, .ctx = list(newdata = df))
}\if{html}{\out{</div>}}

Named arguments in \code{...} take precedence over \code{.ctx} entries with the
same name. If a step declares dependencies (e.g. \code{require_length_matches(~ nrow(newdata))}), the validator checks that all required context is present
before running any steps and errors early if not.
}

\examples{
# Define a validator
require_positive <- restrict("x") |>
  require_numeric(no_na = TRUE) |>
  require_between(lower = 0, exclusive_lower = TRUE)

# Use it
require_positive(5)   # passes silently

# Compose with pipe
require_score <- restrict("score") |>
  require_numeric() |>
  require_length(1L) |>
  require_between(lower = 0, upper = 100)

}
\seealso{
Other core: 
\code{\link{as_contract_block}()},
\code{\link{as_contract_text}()},
\code{\link{fail}()},
\code{\link{require_custom}()}
}
\concept{core}
